# Runtime dependencies for platform builders (Railway Nixpacks, etc.) that
# install from requirements.txt rather than pyproject.toml.
# Mirrors pyproject [project].dependencies + the Postgres driver.
SQLAlchemy>=2.0
pydantic>=2.0
pydantic-settings>=2.0
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
joblib>=1.3
alembic>=1.12
pyarrow>=12.0
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
psycopg[binary]>=3.1
